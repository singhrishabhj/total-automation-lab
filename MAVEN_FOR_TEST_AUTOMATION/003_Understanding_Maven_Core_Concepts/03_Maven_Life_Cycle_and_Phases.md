# Maven Build Life-Cycle

- Mavns's Build lifecycle defines the sequence of phases taht are execitre tyo build and deploy the project . Each phase represent a speicfica stage ion the buil processs, and the phases are executed in predefined order .
- This is the convention over coinfigutaion approach. 
- IN order to just print or exihibit one particular one program, we need to run or execute certain steps. As a programmer there are different stepo you nedd to be performing like you need to compile the code , you need to run unit test cases 

Maven includes three built-in build life cycles: 
1. Clean - The lifecycyle handles the cleaning the build envrionment .
2. Site - This lifecycyle is used to generate projcect documentation and sites reports
3. Defaults - This is the main lifecycle and is responsible for the build and deployment process 